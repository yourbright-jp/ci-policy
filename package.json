{
  "name": "@yourbright/ci-policy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build scripts/check-workflows.ts --target=node --minify --outfile=dist/check-workflows.mjs && bun build scripts/check-pr-body.ts --target=node --minify --outfile=dist/check-pr-body.mjs",
    "check": "bun run scripts/check-workflows.ts",
    "check:coverage": "bun run scripts/check-coverage.ts",
    "test": "bun test"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "4.0.9"
  },
  "packageManager": "bun@1.3.3"
}
