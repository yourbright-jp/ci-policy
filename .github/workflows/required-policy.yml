name: required-policy

on:
  workflow_call:
    inputs:
      repository:
        description: Repository name in owner/name form.
        required: false
        type: string
      policy-ref:
        description: ci-policy self-test ref. Ignored for caller repositories.
        required: false
        default: v6
        type: string

permissions:
  contents: read

jobs:
  policy:
    name: policy
    runs-on: ubuntu-slim
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout target repository
        uses: actions/checkout@v6
        with:
          path: target
          persist-credentials: false

      - name: Checkout pull request base
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: target-base
          persist-credentials: false

      - name: Checkout merge group base
        if: github.event_name == 'merge_group'
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.merge_group.base_sha }}
          path: target-base
          persist-credentials: false

      - name: Checkout push parent
        if: github.event_name == 'push' && github.event.before != '0000000000000000000000000000000000000000'
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.before }}
          path: target-base
          persist-credentials: false

      - name: Checkout policy repository
        uses: actions/checkout@v6
        with:
          repository: yourbright-jp/ci-policy
          ref: ${{ github.repository == 'yourbright-jp/ci-policy' && inputs.policy-ref || 'v6' }}
          path: ci-policy
          persist-credentials: false

      - name: Setup trusted Node runtime
        uses: actions/setup-node@v6
        with:
          node-version: 26.8.1

      - name: Setup Bun for policy self-test
        if: github.repository == 'yourbright-jp/ci-policy'
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version: 1.3.3

      - name: Verify policy distribution
        if: github.repository == 'yourbright-jp/ci-policy'
        working-directory: ci-policy
        run: |
          bun install --frozen-lockfile
          bun run build
          bun test
          git diff --exit-code -- dist

      - name: Validate pull request body
        if: github.event_name == 'pull_request'
        env:
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: node ci-policy/dist/check-pr-body.mjs

      - name: Run policy checks
        env:
          TARGET_REPOSITORY: ${{ inputs.repository || github.repository }}
        run: >-
          node ci-policy/dist/check-workflows.mjs
          --repo "$GITHUB_WORKSPACE/target"
          --base-repo "$GITHUB_WORKSPACE/target-base"
          --repository "$TARGET_REPOSITORY"
